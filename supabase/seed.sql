-- Minimal seed: creates a demo property for a user
-- Register a user via Supabase Auth first, then run:
--   insert into public.property_users (property_id, user_id, role)
--   values ('<property_id>', '<user_id>', 'owner');

-- This file is intentionally empty — property creation happens via the app.